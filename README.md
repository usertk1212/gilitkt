
  # GILI (Yoj)

  This is a code bundle for GILI (Yoj). The original project is available at https://www.figma.com/design/NbSiirR7GckGr3AMoU8qmi/GILI--Yoj-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  